import 'package:flutter/material.dart';

class AppSmallBtn extends StatelessWidget {
  const AppSmallBtn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}